describe("Testing function to retrieve current player counts for games given by array of appids.", function(){

  it("Should gracefully errors returned by the request.");

  it("Should not open more requests than are allowed by the OS.");

  
});
